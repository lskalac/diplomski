var React = require('react');

var AddNote = React.createClass({
  render:function(){
      return (
        <div>
          <h1 className="mainTitle"> New note </h1>
        </div>
      );
  }
});

module.exports = AddNote;
